services:
  db:
    image: postgres:18.4
    environment:
      POSTGRES_USER: kudos
      POSTGRES_PASSWORD: secret
    ports:
      - '5432:5432'
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kudos"]
      interval: 5s
      timeout: 5s
      retries: 5
